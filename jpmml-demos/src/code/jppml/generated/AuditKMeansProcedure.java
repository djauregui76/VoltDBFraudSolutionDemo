package jppml.generated;

import org.voltdb.VoltTable;
import org.voltdb.jpmml.JPMMLImpl;
import org.voltdb.jpmml.VoltDBJPMMLWrangler;

// This code is generated by getUDF
public class AuditKMeansProcedure {

    public String auditKMeans(double Age, double Income, double Deductions, double Hours) {

        final String modelName = "AuditKMeans.xml";
        VoltTable[] pmmlOut = null;
        JPMMLImpl i = null;
        VoltDBJPMMLWrangler w = null;

        try {

            i = JPMMLImpl.getInstance();
            w = i.getPool().borrowObject();
            VoltTable paramtable = w.getEmptyTable(modelName);
            paramtable.addRow(Age, Income, Deductions, Hours);
            pmmlOut = w.runModel(modelName, paramtable);
            return pmmlOut[0].getString(0);

        } catch (Exception e) {

           System.err.println(e.getMessage());
           return null;

        } finally {

          if (i != null && w != null)
              {
              i.getPool().returnObject(w);
              }

        }
    } 

}
